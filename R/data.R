#' @title Exemplo para aplicar funções
#' @description
#' Conjunto de dados contendo variáveis numéricas "y", "x1" e "x2" para testar as funções do pacote.
#' @details
#' O conjunto de dados foi gerado através de simulação, em que as variáveis numéricas foram obtidas
#' através de amostras da distirbuição normal com diferentes parâmetros. Também foi amostrado um erro
#' normal ao produzir a variável "y" por uma equação.
#'
#' @examples
#' data(dados)
"dados"

#' @title Exemplo com variáveis categóricas
#' @description
#' Conjunto de dados contendo variáveis numéricas _"y"_ e _"numerica"_ e variáveis categóricas _"fator1"_ e _"fator2"_ para
#' testar as funções do pacote.
#' @details
#' O conjunto de dados foi gerado através de simulação, em que as variáveis categóricas foram
#' obtidas com uso da função _sample_ e a variável "numerica" foi dada amostrando de uma Poisson deslocada.
#' Também foi amostrado um erro normal ao produzir a variável "y" por uma equação.
#'
#' @examples
#' data(categoricas)
"categoricas"
